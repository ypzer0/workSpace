import * as types from './mutation-types'

export const mutations = {
  [types.INIT_ORDER] (state, o) {
    state.goodsNavList = o.goodsClassNav;
  },
  [types.INIT_GoodsList] (state, o) {
    state.goodsList = o.goodsClasslist;
  },

  
}
