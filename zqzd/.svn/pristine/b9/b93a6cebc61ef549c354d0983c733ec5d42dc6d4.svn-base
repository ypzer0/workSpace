import * as types from './mutation-types'

export const mutations = {
  [types.INIT_GOODS_CLASSIFY] (state, o) {
    state.goodsClassifyNavList = o;
  },
  [types.INIT_GOODS_CONTENT] (state, o) {
    state.goodsListContent = o;
  },

  
}
